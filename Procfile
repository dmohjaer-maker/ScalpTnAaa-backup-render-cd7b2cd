# NOTE: This project is deployed on Render using render.yaml — NOT this Procfile.
    # The two services below are defined as separate web services in render.yaml.
    # This Procfile is kept only as a reference for local development.
    #
    # To run locally (single machine, file-based IPC):
    #   robot: python live_trading/server.py
    #   panel:  python telegram_panel/server.py
    